# audio_smooth

Python + browser toolkit for extracting, denoising, and cleaning recorded speech (see `CLAUDE.md` for pipeline steps).

Set `OPENROUTER_API_KEY` in your environment when using `remove_filler_llm.py` or the server’s LLM filler-removal path.
